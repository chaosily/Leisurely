# Raw sources

Drop source documents here — articles (markdown via Obsidian Web Clipper works well), papers, notes, data files. Images referenced by sources go in `assets/`.

This directory is the immutable source of truth: the LLM reads from it but never modifies it. To process a new source, add it here and ask the LLM to ingest it (see `../CLAUDE.md`).
